{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build source",
            "type": "shell",
            "command": "${workspaceFolder}\\build\\build.bat",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}